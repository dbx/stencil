package io.github.erdos.stencil.test;

import io.github.erdos.stencil.PreparedTemplate;
import io.github.erdos.stencil.Process;
import org.junit.Test;

import java.io.File;
import java.io.IOException;
import java.net.URI;
import java.net.URISyntaxException;
import java.net.URL;

public class TestsFromFiles {

    /*
    @Test
    public void test1() throws URISyntaxException, IOException {
        URL input = this.getClass().getClassLoader().getResource("tests/test_1_input.xml");
        File f = new File((URI) input.toURI());

        Process proc = new Process(new File("/usr/lib64/libreoffice"));
        PreparedTemplate prepared = proc.makeTemplate(f);

        System.out.println(prepared.getSecretObject());

    }
    */
}
